const unassignedConfig = {
    configurationName: 'New Configuration',
    assignee: null,
};

const assignedConfig = {
    configurationName: 'Configuration 1',
    assignee: 'a',
};

export { unassignedConfig, assignedConfig };
